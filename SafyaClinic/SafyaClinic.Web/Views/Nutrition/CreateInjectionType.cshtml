@model CreateInjectionTypeDto
@{
    ViewData["Title"] = "Add Injection Type";
}

<div class="card" style="max-width:600px;">
    <div class="card-header"><i class="bi bi-syringe me-2"></i>Add Injection Type</div>
    <div class="card-body">
        <form asp-action="CreateInjectionType" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2"></div>

            <div class="mb-3">
                <label asp-for="InjectionName" class="form-label">Name *</label>
                <input asp-for="InjectionName" class="form-control" required />
            </div>
            <div class="mb-3">
                <label asp-for="Unit" class="form-label">Unit * <small class="text-muted">(mg, ml, IU, units...)</small></label>
                <input asp-for="Unit" class="form-control" required />
            </div>
            <div class="mb-3">
                <label asp-for="DefaultDosage" class="form-label">Default Dosage</label>
                <input asp-for="DefaultDosage" class="form-control" />
            </div>
            <div class="mb-3">
                <label asp-for="Description" class="form-label">Description</label>
                <textarea asp-for="Description" class="form-control" rows="2"></textarea>
            </div>

            <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg me-1"></i>Save</button>
            <a asp-action="InjectionTypes" class="btn btn-outline-secondary">Cancel</a>
        </form>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
